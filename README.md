# Braille-Translator
CyberCrimeInvestigation is a Java hacker directory built on a dynamic hash table with separate chaining. Load suspects from file, add/update by merging incidents, search/remove by name, and auto-resize via rehashing. Includes alias merging, top-N ranking with MaxPQ, and location-based lookup.
